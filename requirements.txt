# Core dependencies
qdrant-client>=1.7.0
python-dotenv>=1.0.0

# LLM providers (at least one required)
openai>=1.0.0  # For OpenAI and OpenRouter

# Local embeddings (optional but recommended)
sentence-transformers>=2.2.0
torch>=2.0.0  # Required by sentence-transformers
